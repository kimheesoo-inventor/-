---
description: 전체 회의 — 초안 완성 후 각 에이전트가 서로의 산출물을 교차 점검 (파이프라인 4단계)
argument-hint: <슬러그>
---

`CLAUDE.md` 4.4 전체 회의를 실행한다. 대상: $ARGUMENTS

초안이 다 나온 뒤, **만든 사람이 아닌 다른 눈으로** 오류를 한 번 더 훑는 자리다.

## 진행
1. `CLAUDE.md`를 읽는다.
2. `01-research.md` / `02-content.md` / `03-design/` 가 전부 있는지 확인한다. 하나라도 없거나 비어 있으면 회의를 열지 말고 그 사실을 보고한다.
3. `research` · `content` · `design` 서브에이전트를 **교차 점검 모드**로 호출한다.
   각 에이전트에게 `CLAUDE.md` 4.4 렌즈와 **자기 산출물은 보지 않는다**는 규칙을 함께 전달한다.
   세 호출은 서로 독립적이므로 **한 번에 병렬로** 보낸다.
4. `pm` 서브에이전트를 모드 4로 호출해, 세 에이전트의 발견과 PM 자신의 브리프 대조 결과를 `04-crosscheck.md`로 취합한다.

## 규칙
- **회의에서 아무도 고치지 않는다.** 발견 목록만 만든다.
- 발견이 0건이면 "발견 없음"으로 기록한다. 채우려고 억지 지적을 만들지 않는다.
- 발견 항목은 **담당 단계로 반송**해 처리한 뒤 검수로 넘긴다. 미처리 항목을 안고 5단계로 가지 않는다.
- 반송은 최대 2회. 3회째면 멈추고 사용자에게 보고한다.

## 보고
발견 건수, 담당 단계별 분포, 주제 부합 여부(핵심 메시지·"하지 않을 것"·성공 기준)를 요약한다.
