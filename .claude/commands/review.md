---
description: 배포 전 검수 — 통과/보류 판정 (파이프라인 5단계)
argument-hint: <슬러그>
---

`CLAUDE.md` 4.5 를 단독 실행한다. 대상: $ARGUMENTS

1. `CLAUDE.md`를 읽는다.
2. `review` 서브에이전트를 호출한다. **네가 직접 고치지 않는다** — 검수는 판정만 한다.
   `04-crosscheck.md`가 있으면 함께 넘긴다. 회의에서 이미 처리된 항목은 다시 지적하지 않는다.
3. 산출물: `drafts/<슬러그>/05-review.md`
4. 판정에 따라:
   - **통과** → 산출물을 `output/<슬러그>/`로 복사한다.
   - **보류** → `output/`으로 옮기지 않는다. 문제 목록의 `담당 단계`를 그대로 보고한다.
5. 보고에 판정, 보류 건수, 각 건의 담당 단계를 포함한다.
