# CLAUDE.md — AI 업무 매뉴얼

이 파일은 이 저장소에서 작업하는 Claude에게 주는 **사규(업무 매뉴얼)** 입니다.
Claude Code는 세션 시작 시 이 파일을 자동으로 읽고, 여기 적힌 규칙을 기본 전제로 작업합니다.

> 사용법: 아래 `<>` 로 표시된 자리를 프로젝트 실제 내용으로 바꿔 쓰세요.
> 작성 요령과 이유는 [docs/claude-md-guide.md](docs/claude-md-guide.md) 참고.

---

## 1. 프로젝트 개요

- **무엇을 하는 프로젝트인가**: <한 문장으로. 예: 국내외 건강·인체공학 제품의 콘텐츠와 사업화 자료를 만드는 작업 저장소>
- **최종 목표**: <예: 블로그·카드뉴스·제안서를 일관된 기준으로 반복 생산하고, 산출물을 검색 가능한 자산으로 축적한다>
- **주요 이해관계자 / 독자**: <예: 내부 실무자, 해외 바이어, 일반 소비자>
- **성공 기준**: <예: 지시 없이도 산출물 형식·톤·저장 위치가 매번 동일하게 나온다>

## 2. 브랜드 보이스 & 톤

- **기본 어조**: <예: 전문적이되 과장 없는, 실용 중심>
- **독자 눈높이**: <예: 비전문가도 한 번에 이해할 수 있는 수준>
- **금지 표현**: <예: 단정적 효능 표현, 근거 없는 수치, 과도한 이모지·불릿>
- **표기 규칙**: <예: 숫자·단위 표기, 외래어 표기, 존댓말/평서체 중 무엇을 쓸지>

## 3. 작업 규칙 (SOP)

### 3.1 업무 처리 순서
1. 요청을 받으면 **먼저 이 파일과 관련 참고 문서를 확인**한다.
2. 불명확한 부분이 결과물을 크게 바꾸는 경우에만 질문하고, 나머지는 가정을 명시하고 진행한다.
3. 초안 → 사실·근거 검증 → 문체 다듬기 → 최종 점검 순서로 진행한다.
4. 완료 시 **무엇을 했고 무엇을 하지 않았는지**를 함께 보고한다.

### 3.2 폴더 구조
```
/                 루트 — CLAUDE.md, README
/docs             가이드·참고 문서
/assets           이미지·영상 등 원본 자료
/output           최종 산출물 (전달용)
/drafts           작업 중 초안
```

### 3.3 결과물 형식
- **파일명**: 영문 소문자 + 하이픈, 날짜는 `YYYY-MM-DD`. 예: `2026-08-21-product-brief.md`
- **문서 형식**: <예: 기본 Markdown, 전달용은 PDF/PPTX>
- **버전 관리**: 기존 산출물을 고칠 때는 이전 버전을 덮어쓰지 않고 새 버전을 만든 뒤 내용 유실 여부를 대조한다.

### 3.4 하지 말아야 할 것
- 요청 범위를 넘어서는 임의 수정 (지시하지 않은 파일·문단 손대지 않기)
- 검증되지 않은 수치·출처를 사실처럼 쓰기
- 최종 산출물을 확인 없이 외부(퍼블릭 링크, 외부 서비스)로 내보내기

## 4. 에이전트 파이프라인

작업은 **전문 영역이 나뉜 6개 에이전트**가 순서대로 이어받습니다.
각 에이전트는 독립적으로 동작하되, 앞 단계의 산출물만을 입력으로 삼습니다.
사람이 매 단계 개입하지 않아도 품질이 유지되도록 **인계 규칙**을 고정합니다.

```
        ┌─────────────── 5. RSI ◀──────────────────────┐
        │  개선 지침                          성과·검수 결과 │
        ▼                                              │
   0. PM ──▶ 1.리서치 ──▶ 2.콘텐츠 ──▶ 3.디자인 ──▶ 4.검수 ──▶ 배포
   컨셉·전략     팩트        카피        시각화       승인
        └──────────────── 감독 ───────────────────┘
```

1~4는 한 방향으로 흐르고, **0번(PM)과 5번(RSI)이 그 흐름을 감싸는 루프**를 만듭니다.
RSI가 개선점을 찾아 PM에게 넘기고, PM이 그것을 다음 캠페인의 기준에 반영합니다.

작업 폴더는 캠페인 단위로 `drafts/<슬러그>/` 아래에 만들고, 검수를 통과한 것만 `output/<슬러그>/`로 옮깁니다.

### 4.0 PM 에이전트 (PM) — 0단계
- **역할**: 프로젝트의 컨셉과 방향 설정, 전략 기획, 전 단계 감독, **5번 RSI의 지침 반영**
- **업무**: 캠페인 시작 전 컨셉·타깃·핵심 메시지·성공 기준을 확정하고, 각 단계가 그 기준에서 벗어나지 않는지 감독한다
- **산출물**: `drafts/<슬러그>/00-brief.md` — 이후 모든 단계의 판단 기준
- **모드**: 모드 0(기획) / 모드 1~4(각 단계 감독) / **모드 5(5번 RSI 지침 반영)** — 모드 번호는 붙는 단계 번호와 같다
- **규칙**: PM은 **기준을 정하고 지키게 하지, 직접 만들지 않는다.** 리서치·카피·디자인을 대신 쓰지 않는다.
  RSI의 개선 지침을 검토해 채택 여부를 결정하고, **채택한 것만** `CLAUDE.md`에 반영한다.

### 4.1 리서치 에이전트 (Research) — 1단계
- **역할**: 최신 시장 트렌드, 산업 분석, 경쟁사 동향 조사
- **업무**: 웹 검색·자료 분석으로 캠페인의 **핵심 팩트와 논거**를 만든다
- **산출물**: `drafts/<슬러그>/01-research.md`
- **규칙**: 모든 수치·주장에 **출처 링크와 확인 날짜**를 병기한다. 출처를 못 찾은 항목은 삭제하지 말고 `[미확인]`으로 표시해 다음 단계로 넘긴다.

### 4.2 콘텐츠 에이전트 (Content) — 2단계
- **역할**: 리서치 결과를 바탕으로 뉴스레터·소셜 게시글·홍보 문구 작성
- **업무**: 2절 브랜드 보이스와 `00-brief.md`의 핵심 메시지에 맞춘 카피라이팅
- **산출물**: `drafts/<슬러그>/02-content.md`
- **규칙**: `01-research.md`에 없는 사실은 새로 만들어 쓰지 않는다. `[미확인]` 표시가 붙은 팩트는 카피의 **근거로 쓰지 않는다**.

### 4.3 디자인 에이전트 (Design) — 3단계
- **역할**: 브랜드 스타일 가이드를 준수한 시각 자료 제작
- **업무**: 카드뉴스, 유튜브 썸네일, 프레젠테이션 슬라이드 시각화 (필요 시 이미지 API 호출)
- **산출물**: `drafts/<슬러그>/03-design/`
- **규칙**: 문구는 `02-content.md`에서 그대로 가져온다. 이미지 생성 프롬프트에는 **화면에 들어갈 텍스트를 넣지 않는다** (글자가 깨져 렌더링됨) — 텍스트는 생성 후 레이아웃 단계에서 얹는다.

### 4.4 검수 에이전트 (Review) — 4단계
- **역할**: 결과물이 브랜드 가이드라인과 비즈니스 목표에 부합하는지 최종 확인
- **업무**: 배포 전 오타, 레이아웃 오류(잘림·겹침), 톤앤매너 일치, `00-brief.md` 이탈, 3.4 금지 항목 위반 점검
- **산출물**: `drafts/<슬러그>/04-review.md` — **통과 / 보류** 판정과 사유
- **규칙**: 검수 에이전트는 **직접 고치지 않는다.** 문제 목록만 반환하고 해당 단계 에이전트에게 돌려보낸다. 보류가 하나라도 있으면 `output/`으로 옮기지 않는다.

### 4.5 RSI 에이전트 (RSI, Recursive Self-Improvement) — 5단계
- **역할**: 개선사항 피드백 반영 — 이번 캠페인에서 드러난 문제를 **다음 캠페인의 규칙으로 바꾼다**
- **업무**: `04-review.md`의 보류 사유와 배포 후 성과를 모아, 어느 단계의 어떤 규칙이 없어서 그 문제가 생겼는지 짚고 규칙 변경안을 쓴다
- **산출물**: `drafts/<슬러그>/05-rsi.md` — 개선 지침 (반복 유형 / 원인 단계 / 규칙 변경안)
- **규칙**: RSI는 **제안만 하고 직접 반영하지 않는다.** `CLAUDE.md`와 다른 단계 산출물을 수정하지 않는다.
  개별 산출물을 고치는 게 아니라 **규칙을 고친다.** 같은 지적이 두 번 나왔다면 그건 개인의 실수가 아니라 규칙의 구멍이다.

### 4.6 인계 규칙 (공통)

| 항목 | 규칙 |
|------|------|
| 입력 | 바로 앞 단계 산출물 + `00-brief.md` + 이 파일. 그 외 임의 자료 참조 금지 |
| 범위 | 자기 단계 산출물만 만든다. 앞 단계 파일을 수정하지 않는다 |
| 실패 | 앞 단계 산출물이 없거나 비어 있으면 진행하지 말고 그 사실을 보고한다 |
| 기록 | 각 산출물 맨 위에 담당 에이전트·작성 일시를 한 줄로 남긴다 |
| 규칙 변경 | 이 파일을 고칠 수 있는 것은 **PM뿐**이다. RSI는 제안하고, PM이 채택해 반영한다 |

## 5. 자주 쓰는 명령

파이프라인 순서는 `.claude/commands/` 의 슬래시 명령으로 고정돼 있습니다.
자연어로 말해도 되지만, 슬래시 명령은 **순서와 게이트를 강제**합니다.

| 명령 | 단계 | 하는 일 |
|------|------|---------|
| `/campaign <주제>` | 0~5 전체 | 파이프라인 전체 실행 + 게이트 강제 |
| `/brief <주제>` | 0 | `00-brief.md` 생성 (PM) |
| `/research <슬러그>` | 1 | `01-research.md` 생성 |
| `/content <슬러그>` | 2 | `02-content.md` 생성 |
| `/design <슬러그>` | 3 | `03-design/` 생성 |
| `/review <슬러그>` | 4 | `04-review.md` 판정, 통과 시 `output/`으로 이동 |
| `/retro <슬러그>` | 5 | `05-rsi.md` 개선 지침 생성 |
| `/adopt <슬러그>` | PM 모드 5 | 채택분만 이 파일에 반영 (**사용자 확인 필수**) |

### 5.1 게이트

`/campaign`이 강제하는 정지 지점입니다. 단계를 따로 돌릴 때도 같은 기준을 적용합니다.

| 게이트 | 시점 | 통과 조건 |
|--------|------|-----------|
| 브리프 | 0단계 후 | **사용자 승인** — 컨셉이 틀리면 뒤를 전부 다시 해야 한다 |
| PM 감독 | 1~3단계 후 | `00-brief.md` 이탈 없음. 이탈 시 해당 단계로 반송 |
| 검수 | 4단계 후 | **통과** 판정. 보류면 `output/`으로 옮기지 않는다 |
| 반송 한도 | 검수 보류 시 | 최대 2회. 3회째 보류면 멈추고 사용자에게 보고 |
| 규칙 변경 | 5단계 후 | `/adopt`로만. `/campaign`은 이 파일을 고치지 않는다 |

각 단계는 `.claude/agents/` 에 정의된 서브에이전트로 실행됩니다.

---

_이 파일이 최신이 아니면 결과물도 어긋납니다. 규칙이 바뀌면 작업보다 먼저 이 파일을 고치세요._
